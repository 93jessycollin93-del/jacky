# Secrets — never commit
.env
.env.*
!.env.template
secrets/
*.secret
*.key

# Python
__pycache__/
*.pyc
*.pyo

# Data / logs
data/
*.log
*.err
*.db

# Tunnel binary + runtime artifacts (don't commit the 52MB exe)
bin/
tunnel.log*
CURRENT_PUBLIC_URL.txt
